{% if multiple %}
    {% for item in items %}
        {{ item.content }}
    {% endfor %}
{% else %}
    {% for item in items %}
        <h1 {{ attributes.addClass('visually-hidden') }}>{{ item.content }}</h1>
    {% endfor %}
{% endif %}